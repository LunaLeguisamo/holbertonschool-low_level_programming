#include "main.h"

/**
 * _islower - main
 *
 * Return: 0
 *
 * @c: Returns 1 if the input is lowercase
 * @c: Returns 0 otherwise
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
	else
	{
		return (0);
	}

	return (0);
}
