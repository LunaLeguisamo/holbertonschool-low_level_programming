#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - main
 *
 * Return: 0
 */

int main(void)
{
	int numbers;

	for (numbers < 10; numbers++;)
	{
		putchar(numbers);
	}

	return (0);
}
